package controller

const (
	sessionName        = "session"
	sessionSessionID   = "sessionID"
	sessionDisplayName = "displayName"
	sessionNavActive   = "navActive"
)

var sessionFields = [...]string{sessionDisplayName, sessionNavActive}
